[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
markers =
    regression: locked-in known-good DE numbers (uses local datacache/de; skipped if absent)
    integration: needs local datacache
